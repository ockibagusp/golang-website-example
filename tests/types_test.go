package tests

// test flash message: struct
type flash struct {
	success_message string
	error_message   string
}

// test regex: struct
type regex struct {
	must_compile string
	actual       string
}
